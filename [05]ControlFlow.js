/*
    Control Flow atau (alur kendali) 
    IF ELSE DAN FOR LOOP

    Normal Flow
        jika program memiliki lebih dari 1 statment maka dia akan di eksekusi dari ata ke bawah dan dari kiri ke kanan
        seperti panah dari atas ke bawah

    PENGULANGAN:
    Pengulangan pada javascript memiliki 3 jenis yaitu
        for (var i = 0; i < 10; i++)
        while (kondisi)
        do{code} while(kondisi)

    PENGKONDISIAN:
        if (kondisi) {CODE}
        if (kondisi) {CODE} else {CODE JIKA TIDAK SESUAI DENGAN IF}
        switch (KONDISI YANG AKAN DI EKSEKUSI) case(KONDISI JIKA TERPILIH)
*/