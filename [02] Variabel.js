/*
    java scrpt memiliki 3 buah variabel yaitu 
        var = digunkan untuk membuat varuabel yang bisa di ubah nilainya dan dapat di akses oleh seluruh program
                intinya dapat di akses oleh seluruh block code 
        let = digunakan untuk membuat variabel yang mana kita bisa merubah nilai dari variabel tsb tetapi tidak dpatdi akses oleh seluruh program 
                intinya variabel let dapat mengganti nilai awal tetapi hanya bisa diakses di temaot dimana diai di buat
        const = digunakan untuk membuat variabel yang mana dia tidak dapat di ganti nilai nya dan hanya bisa diakses oleh code program tampat dia di buat
                intinya variabel const tidak dapat di rubah nilainya jika sudah di assigment dan hanya bisa di akses oleh tempat dia di buat

    Deklarasi merupakan sebuha tahap untuk mendaftarkan variabel kedalam lingkup yang sesuai
        var x; inin merupapkan deklarasi dan inisialisasi
    inisialisai merupakan sebuah proses menyedikan memori untuk variabel

    assingment merupakan tahap untuk mentapkan nilai ynag spesifik kedalam sebuah variabel
        x = 10; ini merupkan assingment

    tidak boleh menggunakan spasi
    stntax penulisan variabel 
        TipeVriabel NamaVariabel;
        var namaAwal; ini cemal case yang mana awal nya kecil lalu di ikut dengan nama besar
        let namaAkhir;
        const namaAwal;
*/
